{
  "name": "Platformer",
  "version": "1.0.0",
  "description": "Simple platformer game",
  "main": "index.js",
  "scripts": {
    "start": "npm run build && serve dist",
    "dev": "nodemon --exec 'npm start' -e ts && serve dist",
    "build": "rm -rf dist && tsc --build tsconfig.json && cp src/index.html dist/",
    "test": "jest"
  },
  "author": "Johan",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^26.0.15",
    "jest": "^26.6.1",
    "prettier": "^2.1.2",
    "serve": "^11.3.2",
    "ts-jest": "^26.4.3",
    "typescript": "^4.0.5"
  }
}
